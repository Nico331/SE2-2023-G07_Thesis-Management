import React, { useEffect } from 'react';
import axios from 'axios';

const RequestedProposals = () => {

    useEffect(() => {
        document.title = "Requested Proposals";
    }, []);

    console.log("safaf");
    return (
        <>
            
        </>
    );
};

export default RequestedProposals;   